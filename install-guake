# Install Guake
sudo apt-get install guake

# Configure Guake
sudo mkdir /etc/gconf/schemas
cd /etc/gconf/schemas/
sudo ln -s /usr/share/gconf/schemas/guake.schemas

# Update properties
mv ~/.gconf/apps/guake ~/.gconf/apps/guake-copy
cp guake ~/.gconf/apps/
